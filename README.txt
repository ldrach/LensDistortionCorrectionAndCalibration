Setup:
Put distorted pictures into "images" folder (There are preloaded example images).

In terminal run:
    python calibrate.py "images/*.jpg"

This will return your calibration parameters that will be entered into the array
on line 6 of main.py.

An "output" folder will be created with the corrected images.

Input the calibration parameters and run main.py

